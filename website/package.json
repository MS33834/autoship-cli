{
  "name": "autoship-website",
  "version": "0.1.0",
  "description": "AutoShip landing page",
  "private": true,
  "scripts": {
    "dev": "python -m http.server 8080",
    "build": "node scripts/build.js",
    "serve": "python -m http.server 8080"
  },
  "devDependencies": {}
}
