{
  "name": "autoship-registry-web",
  "version": "1.0.0",
  "private": true,
  "description": "AutoShip plugin registry web UI",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "python -m http.server 8080 --directory dist"
  },
  "license": "MIT"
}
